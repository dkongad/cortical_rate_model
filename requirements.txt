# Environment for the cortical_rate_model package
# ------------------------------------------------
# Python >= 3.9 is required (tested on Python 3.13 in the "myenv" conda env).
#
# The versions below are the ones the package was developed and tested against.
# Newer minor versions are generally fine; the lower bounds are the versions
# that are known to work.
#
# Install into a dedicated environment, e.g.:
#     conda create -n myenv python=3.13
#     conda activate myenv
#     pip install -r requirements.txt

# --- core (required) -------------------------------------------------------
numpy>=1.26          # array maths                (tested 2.1.3)
scipy>=1.11          # eigenvalues, gaussian_filter (tested 1.15.3)
torch>=2.0           # dynamics / integration      (tested 2.6.0)

# --- plotting (optional; only needed for cortical_rate_model.plotting) ------
matplotlib>=3.7      # visualisation               (tested 3.10.0)
